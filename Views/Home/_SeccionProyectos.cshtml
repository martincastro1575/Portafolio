@model IEnumerable<Proyecto>


@if (Model is not null)
{
    <div class="text-center" style="padding-top: 6rem;">
        <h1>Trabajos recientes</h1>
        <p>Encontraras mas de mis proyectos aqui</p>
    </div>

   <partial name="_ListadoProyectos" model="Model" />
}